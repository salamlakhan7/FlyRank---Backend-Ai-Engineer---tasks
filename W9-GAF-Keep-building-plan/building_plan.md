# The Plan to Keep Building — Week 9
**Author:** Abdul Salam

---

## 1. How I'll Add the Next Case Study

**Where it goes:** New `.endpoint` block in the "endpoints // selected work" section of my portfolio site (`index.html`), same pattern as DocuMind/Job Connect/AB Medical/VisionX Tracker.

**Steps to add one** (three-beat shape — problem, what I did, what came of it):
1. Write the case in three beats: the problem the project solves, what I actually built (stack + key decisions), and the concrete outcome (what it does end-to-end, deployed where).
2. Drop it into the same Claude Project used to build the site, so voice/stack/identity kit carry over automatically — no rebuilding context.
3. Paste the new `.endpoint` block into `index.html` (verb/path/status header + h3 + description + tags + GitHub/demo links), matching the existing four.
4. Push to the repo → Vercel auto-redeploys.
5. Test the live URL in a private window before calling it done.

---

## 2. Next Real Piece of Work

**Project:** An AI agent system combining FastAPI + Streamlit + Neon (Postgres) + a RAG pipeline + neural network components — extending my existing backend/RAG work (DocuMind, Job Connect) into genuine agentic behavior rather than fixed workflows.

**Reminder set:** [state what you actually set — e.g. "Recurring calendar reminder every [day] to check progress and update the case study" or "Note added to my task manager for [date]"]

---

## 3. Build Context Preserved

Continuing to use the same Claude Project used for the portfolio site and prior assignments — it already has my voice, stack, and identity kit, so adding this case study will be a short conversation, not a rebuild.
